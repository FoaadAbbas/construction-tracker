# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/construction_tracker?retryWrites=true&w=majority

# JWT secret for authentication (use a long random string)
JWT_SECRET=your-secure-random-secret-key

# Frontend URL for CORS (your Vercel URL)
CORS_ORIGIN=https://your-app.vercel.app

# Optional: Gemini API for AI features
GEMINI_API_KEY=

# Optional: Email (SMTP) configuration for password reset
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
